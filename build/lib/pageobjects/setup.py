from setuptools import setup, find_packages

setup(
    name='demoframework_pageobjects',
    version='1.0',
    packages=find_packages(),
    install_requires=[],  # No module-specific dependencies
)